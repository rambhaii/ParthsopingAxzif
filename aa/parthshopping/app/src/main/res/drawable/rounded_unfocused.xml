<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" />
    <corners android:radius="@dimen/_3sdp" />
    <stroke
        android:width="1dp"
        android:color="@color/back_bg" />
</shape>